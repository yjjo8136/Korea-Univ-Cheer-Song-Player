import 'package:flutter/material.dart';

class LyricDetailPage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Lyric Detail Page')),
    );
  }
}